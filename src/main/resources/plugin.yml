main: ${PACKAGE}.${NAME}
name: ${NAME}
version: "${VERSION}"
api-version: "1.18"
author: AUTHOR
description: Tracks per-player PvP kill and death statistics with PlaceholderAPI support.
soft-depend:
  - PlaceholderAPI
